{
  "name": "dojeon-nlp-worker",
  "version": "1.0.0",
  "private": true,
  "description": "SQS → 바른 AI 형태소 분석 → Prisma NlpJob 업데이트 (Lambda)",
  "scripts": {
    "build": "tsc",
    "prisma:generate": "prisma generate --schema=../../prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "axios": "^1.7.9",
    "ioredis": "^5.4.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "prisma": "^5.22.0",
    "typescript": "^5.7.2"
  }
}
