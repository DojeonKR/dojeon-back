node_modules
dist
.env
.env.local
# 기타 비밀/로컬 전용 env (템플릿은 예외)
.env.*
!.env.example
!.env.docker.example
*.log
coverage
.idea
.vscode/*
!.vscode/settings.json

# TypeScript incremental
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# 실수로 레포에 넣는 키 방지
*.pem

# 수업 시드 JSON (DB에 적재 후 서비스; IP 보호를 위해 Git 미포함)
prisma/data/
